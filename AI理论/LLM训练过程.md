（AI生成，没有验证的知识，我都懒得看）
1. **数据收集**：汇聚网页、书籍、代码、对话等多源语料，明确许可/版权与合规边界，记录来源与时间版本。
2. **数据清洗与去重**：去除乱码、模板页、广告、低质量与敏感内容；按文档/段落/句子级去重，降低训练噪声与数据泄露风险。
3. **数据过滤与配比**：用规则+模型打分筛选质量（语言、长度、毒性、重复率、困惑度等），并按领域/语言/体裁设定采样配比，避免单一来源主导。
4. **数据切分与打包**：将长文切为适合上下文窗口的片段，构造训练样本（连续文本或带边界的样本），打包成高吞吐读取格式（如 shard）。
5. **Tokenizer训练/选择**：训练或选择 BPE/Unigram 等分词器，确定词表大小与特殊符号；影响序列长度、压缩率与多语言表现。
6. **模型与目标函数设计**：确定 Transformer 架构、参数规模、上下文长度、位置编码、归一化/激活等；常用目标为自回归下一词预测（cross-entropy）。
7. **预训练（Pre-training）**：在海量通用语料上进行大规模训练，学习语言与世界知识；配合混合精度、数据并行/张量并行/流水并行提升效率。
8. **训练稳定与正则**：使用学习率预热+衰减、梯度裁剪、权重衰减、dropout、label smoothing 等，减少发散与过拟合并提升泛化。
9. **指令微调（SFT）**：用高质量指令-回答数据进行监督学习，使模型更会“按要求办事”，对齐常见任务格式与对话风格。
10. **偏好对齐（RLHF/DPO 等）**：通过人类偏好数据训练奖励模型并进行强化学习（PPO），或用 DPO/IPO 等直接偏好优化，提高有用性、安全性与一致性。
11. **安全与红队**：构建安全策略与拒答边界，加入安全数据与对抗样本；进行越狱/提示注入/敏感信息等红队测试并迭代修复。
12. **评测与回归**：用通用基准（知识、推理、代码、长上下文）与业务评测集进行离线评测；监控回归与数据泄露，形成可复现报告。
13. **压缩与推理优化**：量化（INT8/INT4）、蒸馏、剪枝、KV cache、推理并行与算子融合，降低延迟与成本并提升吞吐。
14. **部署与监控**：上线后监控质量、延迟、成本、崩溃率与安全事件；记录日志与反馈闭环，驱动后续数据增量与再训练/微调。
